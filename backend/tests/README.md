# tests

Workspace-level integration tests and sample media fixtures metadata (one file per
probe case: H.264 SDR, HEVC HDR10, DV P5, DV P8.1, AV1, H.264 High-10). Populated as
each phase lands its verification (see the **Verification** section of each task file).
